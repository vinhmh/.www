doctype html
html
  head
    title 'iBridge People AudioDesk ®'
    link(rel='shortcut icon' type='image/x-icon' href='/favicon.ico')
    style(type='text/css').
      html, body {padding: 0; margin: 0; height: 100%;}
      body {display: flex;}
      #iframe { margin: 0; padding: 0; height: 100%; width: 100%; border: none; flex: 2; }
    script.
      window.onbeforeunload = function () {
        if (window.opener && !window.opener.closed) {
          window.opener.postMessage('BBB_CLOSED', '*');
        }
      }
  body
    iframe(id='iframe' src=joinUrl allow="microphone; camera")
